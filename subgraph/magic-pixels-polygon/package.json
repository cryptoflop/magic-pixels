{
  "name": "magic-pixels-polygon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ magic-pixels-polygon",
    "create-local": "graph create --node http://localhost:8020/ magic-pixels-polygon",
    "remove-local": "graph remove --node http://localhost:8020/ magic-pixels-polygon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 magic-pixels-polygon",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
