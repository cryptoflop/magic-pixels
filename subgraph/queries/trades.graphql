query AllTradesForAccount($account: ID!) {
	trades(filter: {
    OR: [
      { creator: { equalTo: $account } },
      { receiver: { equalTo: $account } }
    ]}
	) {
    tradeType
    receiver
    price
    pixels
    id
  }
}